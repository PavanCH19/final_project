import { Topology } from './index.js';
import type { PeerId } from 'libp2p-interfaces/peer-id';
import type { PeerData } from 'libp2p-interfaces/peer-data';
import type { Connection } from 'libp2p-interfaces/connection';
import type { Registrar } from 'libp2p-interfaces/registrar';
import type { MulticodecTopologyOptions } from 'libp2p-interfaces/topology';
interface ChangeProtocolsEvent {
    peerId: PeerId;
    protocols: string[];
}
declare const multicodecTopologySymbol: unique symbol;
export declare class MulticodecTopology extends Topology {
    readonly multicodecs: string[];
    constructor(options: MulticodecTopologyOptions);
    get [Symbol.toStringTag](): string;
    get [multicodecTopologySymbol](): boolean;
    /**
     * Checks if the given value is a `MulticodecTopology` instance.
     */
    static isMulticodecTopology(other: any): boolean;
    set registrar(registrar: Registrar | undefined);
    get registrar(): Registrar | undefined;
    /**
     * Update topology
     *
     * @param peerDatas
     */
    _updatePeers(peerDatas: Iterable<PeerData>): void;
    /**
     * Check if a new peer support the multicodecs for this topology
     */
    _onProtocolChange(event: ChangeProtocolsEvent): void;
    /**
     * Verify if a new connected peer has a topology multicodec and call _onConnect
     */
    _onPeerConnect(connection: Connection): void;
}
export {};
//# sourceMappingURL=multicodec-topology.d.ts.map